import styled from "styled-components";

export const Container = styled.button`
	size: 5rem;

	background-color: ${({ theme }) => theme.COLORS.WHITE_HIGHLIGHT};

	margin: 1.4rem 3rem 1.6rem 1.5rem;
	border-radius: 2rem;
`;
