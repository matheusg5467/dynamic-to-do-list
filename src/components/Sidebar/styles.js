import styled from "styled-components";

export const Container = styled.div`
    grid-area: sidebar;
    background-color: ${({ theme }) => theme.COLORS.GREEN_SIDEBAR};
    
    height: 100%;
    width: 30rem;
    
`